/**
 * @deprecated Import from '@/agents' instead.
 * Behavior trees now live in src/agents/<agent-id>/behavior.ts
 */

export { getBehavior, getAllBehaviors } from '../agents'
export { wait, cooldown, repeat } from '../agents'
